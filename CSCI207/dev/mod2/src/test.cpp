#include <iostream>

using namespace std;

int main (){

    //Scope resolution opperator
    //<< Insertion operator - goes into the data stream
    //>> Extraction operator - pulls from the data stream

    std::cout << "This is a not a test";
    std::cout << "\n\t **Queue Halo Theme music**\n";
    
}
//Compile with 
//g++ -std=c++V -g sourcecode -o executablelocation
    //V - version release every three years - most recent 14
// -g adds debugging to the file.
    //Compile time - syntax error
    // Runtime Error - logic error