/* This is the main file for the stencil project for CSCI473
This file will create a 2d matrix, and then iterate across the matrix to simulate the passing of heat
across a metal plate. The heat simulation will be 1 as hot and 0 as cold. The function will take the
average of values of # of neighbors around the point of interest and assign a new value to the point
*/

// Average function

// main function
int main(int argc, char **argv)
{
    // Check if arguments were passed on program call
    // Tell usage if arguments weren't passed
    // Initialize variables and read in values from function call
    // Create 2d matrix using # of mallocs
    // Set boundary values
    // Create loop to iterate user defined number of times
    // Create loop to iterate across the values
    // Iterate across the matrix and change values using double buffer
}